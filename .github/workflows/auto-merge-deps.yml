name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: netresearch/skill-repo-skill/.github/workflows/auto-merge-deps.yml@ec73cafe09fa8f27daedeb86af45db8cc13e7d20 # main
    permissions:
      contents: write
      pull-requests: write
