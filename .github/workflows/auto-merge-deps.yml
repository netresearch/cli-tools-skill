name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: netresearch/skill-repo-skill/.github/workflows/auto-merge-deps.yml@5b3f983ec4a3d9868f26c565f838bf61c7397ce8 # main
    permissions:
      contents: write
      pull-requests: write
