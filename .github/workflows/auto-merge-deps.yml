name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: netresearch/skill-repo-skill/.github/workflows/auto-merge-deps.yml@84dab32b407a17aa7826e30b86b3e9a503c75e32 # main
    permissions:
      contents: write
      pull-requests: write
