name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: netresearch/skill-repo-skill/.github/workflows/release.yml@5b3f983ec4a3d9868f26c565f838bf61c7397ce8 # main
    permissions:
      contents: write
