name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: netresearch/skill-repo-skill/.github/workflows/release.yml@84dab32b407a17aa7826e30b86b3e9a503c75e32 # main
    permissions:
      contents: write
