name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: netresearch/skill-repo-skill/.github/workflows/release.yml@ec73cafe09fa8f27daedeb86af45db8cc13e7d20 # main
    permissions:
      contents: write
