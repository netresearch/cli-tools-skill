name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: netresearch/skill-repo-skill/.github/workflows/release.yml@62b5061255e68d2364cc1112a9b18353f7bc8fdd # main
    permissions:
      contents: write
